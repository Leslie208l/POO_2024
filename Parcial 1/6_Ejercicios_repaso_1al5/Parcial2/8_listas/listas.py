
"""
List (array)
son colecciones o conjuntos de datos/valores bajo 
un mismo nombre,para acceder a los valores se 
hace con un indice numerico

note:sus valores si son modificables

La lista es una coleccion ordenada y modificable,..
permite miembros duplicados
"""

#ejemplo crear una lista de numeros e imprimir el contenido

numeros=23
numeros=23

numeros[23,24 ]
print(numeros)
#recorrer la lista con ciclo for
for i in numeros:
    print(i)

    #recorrer la lista con el ciclo while
    i=0
    tamaño =en(numeros)
    print(tamaño)
    i=0
    while i<=len(numeros):
        print(numeros(i))
        1+1

#ejemplo 2 crear una lista de palabras y posteriormente buscar la coincidencia de una palabra

palabras=[ "naranja","utd","america","azul"]

palabra_buscar=input("ingresa la palabra a buscar:")

for i in palabras:
    if i=palabra_buscar:
       print ("")